{
  "configurations": [
  {
    "name": "CUDA C++: Launch",
    "type": "cuda-gdb",
    "request": "launch",
    "program": "${workspaceFolder}/out",
    "args": ["-i","${workspaceFolder}/test/sample.graph","-v" ,"-mode", "3"]
  }
  ]
}
